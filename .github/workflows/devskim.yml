name: DevSkim Security Scanning (Enhanced)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '18 3 * * 2'  # Weekly on Tuesday at 03:18 UTC

jobs:
  devskim:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run DevSkim scanner
      uses: microsoft/DevSkim-Action@v1
      with:
        path-to-scan: .
        output-file: devskim-results.sarif
        rules: 'security,credentials'  # Enhanced rules for better coverage

    - name: Upload DevSkim scan results to GitHub Security tab
      uses: github/codeql-action/upload-sarif@v4  # Update ke v4
      if: always()
      with:
        sarif_file: devskim-results.sarif